@model thuctap_CTN_NEW.Models.Ad

@{
    ViewData["Title"] = "Delete";
}

<div class="col-lg-12">
    <section class="panel">
        <header class="panel-heading">
            XÓA GIẢNG VIÊN
        </header>
        <div class="panel-body">
            <div class="position-center">
            <form asp-action = "Delete">
                <h3>
                     Bạn Có Muốn Xóa Giảng Viên <b>"@Html.DisplayFor(model => model.AdTen)"</b> ? 
                </h3>
                
                <div class="form-group">
                    <div class="col-lg-offset-2 col-lg-5">
                        <input type="hidden" asp-for="AdId" />
                        <input type="submit" value="Xóa" class="btn btn-primary" />
                        <button type="button" class="btn" style="background-color: tomato;"><a href="/Ads/Index" style="color: white;background-color: tomato;">Quay Trở Lại</a> </button>
                    </div>
                </div>
            </form>
            </div>
        </div>
    </section>
</div>

</div>
